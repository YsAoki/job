import styled from "styled-components";

export const SConditionNameTag = styled.li`
  padding: 0.19rem 0.75rem;
  border: 1px solid ${({theme}) => theme.color.primary};
  border-radius: 0.25rem;
  font-size: 0.75rem;
`